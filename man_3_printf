_printf(3)      – Custom Output Formatting Function


NAME
    _printf – Displays formatted text based on specifiers

SYNOPSIS
    #include "main.h"  

    int _printf(const char *format, ...);  

DESCRIPTION
    _printf is a function that prints a formatted string, similar to the printf function in C. 
    It supports several format specifiers to display different types of data.

    Supported Format Specifiers:

    %c : Displays a character
    %s : Displays a string
    %d : Displays a decimal number
    %i : Same as %d
    %% : Displays a literal %

RETURN VALUE

    Returns the number of characters printed (excluding the null terminator '\0').
    Returns -1 if format is NULL.

EXAMPLES
    Displaying a character:

        _printf("Character: %c\n", 'A');  

    Displaying a string:

        _printf("Message: %s\n", "Hello!");  

    Displaying a number:

        _printf("Number: %d\n", 42);  

    Displaying a percent sign:

        _printf("Progress: %%\n");  


AUTHORS

      Written by Nina Galasso and Omar Rouigui.


